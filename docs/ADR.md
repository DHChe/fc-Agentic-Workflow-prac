# Architecture Decision Records

결정 기록은 `docs/ARCHITECTURE.md` 12절 "결정 기록 (ADR 요약)" 표에 한 곳으로 모아 둔다. 여기에 따로 적지 않는다. 새 결정이 생기면 그 표에 번호를 이어서 추가한다.

철학: 시연용 MVP. 관리형 무료 서비스(Vercel, Clerk, Neon, Vercel Blob, Claude API)에 기대고 직접 운영하는 인프라는 두지 않는다. 장치는 하나로, "나중을 위한" 방어와 추상화는 넣지 않는다. 잘라낸 항목은 `docs/PRD.md` 10절 제외 목록에 이유와 함께 남긴다.
